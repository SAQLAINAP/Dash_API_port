# This file serves as a explicit place for Conflict logic or models if needed strictly separately.
# Currently, Conflict model is defined in registry.py for cohesion with the JSON structure.
# We can re-export it here or add conflict-specific logic.

from app.models.registry import Conflict

# Future: Conflict resolution strategies?
